<div class="modal fade" id="confirmSendTimetablesModal" tabindex="-1" aria-labelledby="sendModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-sm">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="sendModalLabel">
                    <i class="bi bi-envelope-paper"></i> Confirm Timetable Dispatch
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body text-center">
                <p>Are you sure you want to send the updated timetables to all your students?</p>
                <p class="text-muted"><small>This will email each student their personalized schedule.</small></p>
            </div>

            <div class="modal-footer justify-content-center">
                <form asp-action="SendTimetables" method="post">
                    <button type="submit" class="btn btn-success px-4">
                        <i class="bi bi-send"></i> Yes, Send Timetables
                    </button>
                </form>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>
